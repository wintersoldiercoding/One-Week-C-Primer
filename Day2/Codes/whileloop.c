#include <stdio.h>

int main() {
    int num = 1;

    // Loop until num becomes greater than 5
    while (num <= 5) {
        printf("Value of num: %d\n", num);
        num++; // Increment num for the next iteration
    }

    return 0;
}
